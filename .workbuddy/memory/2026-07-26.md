# 2026-07-26

## 架构分析交付
- 输出五维度架构分析报告 `docs/architecture-report.html` + 三张架构图 `docs/diagrams/*.svg`（分层架构 / 模块依赖 / SM-2 数据流）。

## 让 WorkBuddy 复用 .trae 扩展
- 在 `~/.workbuddy/mcp.json` 注册 `vocabcraft-mcp`（stdio，绝对路径 uv 启动）。
- 将 `.trae/skills/` 下 5 个 vocabcraft-* skill 复制到 `.workbuddy/skills/`（项目级）。
- 验证：`.venv` python 可正常 `import vocabcraft_mcp.server`（main 存在）。
- 待用户操作：连接器管理页面对 `vocabcraft-mcp` 点击 Trust 激活；commands/agents 由 skill 覆盖，rules 无自动注入需靠 SKILL.md 内联。
