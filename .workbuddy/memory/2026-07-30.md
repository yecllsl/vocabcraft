# 2026-07-30

## 词汇统计（vocabcraft-stats）
- 调用 `get_statistics` 跑出当前状态：词汇总数 130；掌握度=新词35/生疏95/已掌握0；语种=de 113 / zh_classical 17；录入日期集中在 07-24(117)、07-28(12)、07-25(1)；历史出题 拼写115 / 释义32（147次）。
- `schedule_review`(无参) 返回 due_count=129/130，几乎全部词今天到期——建库后尚未真正跑过复习。
- 发现并修正 skill 文档：旧 `vocabcraft-stats` 写的 `overview`/`review` 维度服务端已不支持，实际只支持 `date`/`language`/`mastery`/`quiz_type`；掌握度实际返回 新词/生疏（无"已掌握"）。已更新 SKILL.md 的 Workflow 与 Quick Reference。

## 算法对比与文档修正
- 用户问 VocabCraft(SM-2) vs Anki(FSRS) 算法对比。结论：SM-2 够用省心、FSRS 数据越多越强。
- 修正两处文档/注释不一致：
  1. AGENTS.md 复习规则第1条：原"遗忘曲线参数取自 forgetting_curve.json"改为准确说法——排程由 `algorithms.py` 改良版 SM-2 驱动，json 仅用于 Web 画曲线图、不参与计算。
  2. `algorithms.py` docstring：原"grade 3 同 0-2 视为失败"改为"grade 3 视为通过（SM-2 标准 q>=3 即成功）"，与代码 `if grade < 3` 一致。
  - 编译校验通过（仅注释/文档改动，无行为变化）。
  - 注：`forgetting_curve.json` 内部注释仍引用不存在的 `algorithms.ebbinghaus_schedule`，用户未要求改，留待后续。

## 修正：forgetting_curve.json 实际是死配置（非"仅用于 Web"）
- 用户追问"json 仅用于 Web 画曲线图是否说明 Web 展示不准确"→ 进一步核查发现：**`forgetting_curve.json` 完全未被任何 .py 读取**（grep 全仓无 json.load/路径引用）。Web 曲线由 `web/services.py` 的 `get_forgetting_curve()`（合成参考线：`retention=max(35,100-(i+1)*12)` 落在 [1,2,4,7,15] 天 → 88/76/64/52/40%，docstring 自承"非真实艾宾浩斯公式，仅用于可视化参考"）+ `_real_retention_curve()`（基于 ReviewRecord 真实 grade 计算，无复习记录时返回空）生成。
- 结论：我上轮改 AGENTS.md 写的"json 仅用于 Web 画曲线图"也是错的，已更正为"死配置"；MEMORY.md 同步修正。
- Web 展示评估：theoretical 线本质是装饰性占位（递减形状任意、非真实模型，但 docstring 已声明仅参考）；real 线数据驱动、准确，但当前 0 次复习→为空。整体"诚实但粗糙"，风险是用户易把 theoretical 线误认成自己的保留率。
