# 2026-08-04 工作日志

## vocabcraft-capture：录入文言文词汇「负」
- 用户通过对话附件上传 `091.png`（文言文实词卡片，词「负 fù」，动词，10 义项）。
- 已完成：多模态读取 → 结构化 → 用户确认 → `save_vocab` 保存。
- vocab_id：`vocab_20260804_005`，路径 `vocabcraft-mcp/data/vocabs/vocab_20260804_005.json`。
- 结构：语言 `zh_classical`，每个 Definition 带 `part_of_speech=动词` 及其归属例句（篇名用《》括起附在例句后）。
- 注意：Read 工具对 HEIC 报错「Cannot display content of binary file」，需转 PNG 才能读。

## vocabcraft-capture：批量录入 092–095 四词
- 用户转好 PNG 后置 `data/images/zh_classical/`：092 复/093 赋/094 盖/095 干。
- 流程：Read 图片 → 结构化（每义项标词性、例句按义项归属）→ 用户复核词性 → 确认 → 并行 `save_vocab` ×4。
- 保存结果：
  - `vocab_20260804_006` 复 fù（动/形/副，11 义项）
  - `vocab_20260804_007` 赋 fù（名/动，6 义项）
  - `vocab_20260804_008` 盖 gài（名/动/副/连/助，11 义项；含 hé 音义项）
  - `vocab_20260804_009` 干 gān（名/动，7 义项）
- 用户明确要求：每个例句必须展示对应词性以便核对（防义项词性错配）。

## vocabcraft-capture：批量录入 096–100 五词
- 用户转好 PNG：096 更/097 苟/098 鼓/099 故/100 固。
- 流程同上：Read → 结构化（义项标词性）→ 用户复核 → 修正 → 并行 save_vocab ×5。
- 修正点：用户指出「固」的「坚持，坚决，执意」义项（吾义固不杀人/少年固强之）应为副词，原错归动词；已改正后保存。
- 保存结果：
  - `vocab_20260804_010` 更 gēng（动/量/副，10 义项）
  - `vocab_20260804_011` 苟 gǒu（副/连，4 义项）
  - `vocab_20260804_012` 鼓 gǔ（名/动，7 义项）
  - `vocab_20260804_013` 故 gù（名/形/副/连，11 义项）
  - `vocab_20260804_014` 固 gù（形/动/副/连，14 义项，含「坚持坚决执意」修正为副词）
- 累计今日录入：负(005)+复赋盖干(006-009)+更苟鼓故固(010-014) = 共 10 词。

## HEIC 转图环境
- 环境无 ImageMagick/ffmpeg/pillow。已建隔离 venv：`C:\Users\yecll\.workbuddy\binaries\python\envs\heif`（pillow + pillow-heif），可 `pillow_heif.register_heif_opener()` 后将 HEIC 转 PNG。
- `zh_classical/` 下另有 26 张 HEIC（61-65,71-75,076-090）待处理，尚未转图。已告知用户可批量转图或逐张上传 PNG。
