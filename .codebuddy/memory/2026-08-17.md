# 2026-08-17

## 一致性审计（重大调整后）
- 重大调整：`.agents/` → `vocabcraft.plugin/`，MCP Server 内联 `vocabcraft.plugin/vocabcraft-mcp/src/vocabcraft_mcp/`，data 随包落 `vocabcraft.plugin/vocabcraft-mcp/data/`（.gitignore 忽略，git 未跟踪）。
- 结论：核心一致。版本号全 0.6.2；server 注册 11 工具与 tools.json 一致；runtime json 与脚本均指向 vocabcraft.plugin/；无残留 `.agents/` 路径引用；root AGENTS.md == plugin AGENTS.md（逐字节相同）。
- 需打磨文档：A) AGENTS/README 的 MCP Tools 参考表缺 query_vocab/update_vocab/delete_vocab 3 个 CRUD；B) CHANGELOG[0.5.5] 称清理 WorkBuddy 引用但当前 README/DEPLOY/QUICKSTART/install 仍大量引用 WorkBuddy/Hermes（且 CHANGELOG 第68行与86行自相矛盾）；C) generate_quiz 描述缺"文言文释义"；D) generate-aaif-declarations.py docstring 示例路径误写 `../scripts/`；E) build-release 默认版本硬编码未受 check_version 监控。

## 已应用修正（用户决策：B 保留 WorkBuddy/Hermes 个人级）
- A 已修：`vocabcraft.plugin/AGENTS.md` 与根 `AGENTS.md` 的 MCP Tools 参考表补全 query_vocab/update_vocab/delete_vocab，现列全 11 个工具（两文件同步）。
- B 已修：`CHANGELOG.md` 澄清「.workbuddy/→.codebuddy/」为项目级目录重命名，个人级 WorkBuddy/Hermes 仍保留（~/.workbuddy / ~/.hermes）；删除误称"WorkBuddy 已重命名、仅生成 .codebuddy/"的矛盾表述。
- C 已修：`server.py` / `tools.json` / `workflows.json` 的 generate_quiz 描述补全"文言文释义"，全局无旧描述残留（grep 验证）。
- D 已修：`generate-aaif-declarations.py` docstring 示例路径 `../scripts/` → `../../scripts/`。
- 验证：`scripts/check_version.py` 通过（0.6.2 全一致）。
- 未动（低风险维护项）：E build-release 默认版本硬编码；README 结构未列 plugin.json/mcp.json；docs/ 与 .kilo 未被文档引用。

## 续：E + 镜像刷新 + 发布 0.7.0（用户决策：保留 WorkBuddy/Hermes 个人级）
- E 已修：`build-release.ps1`/`.sh` 版本默认值改为从 `vocabcraft.plugin/vocabcraft-mcp/pyproject.toml` 动态读取（消除硬编码漂移）；两脚本语法校验通过。
- 版本提升到 0.7.0：pyproject.toml / plugin.json / package.json / README / DEPLOY / CHANGELOG([Unreleased]→[0.7.0] 并补"文档与脚本一致性"小节) 全部统一；QUICKSTART 用占位符无需改。
- 镜像刷新：`scripts/sync-agent-configs.ps1` 重跑 → 重新生成 tools.json/triggers.json/workflows.json（0.7.0 + 文言文释义），并将 skills + AGENTS.md（11 工具表）单向同步到 .trae/.opencode/.codebuddy/.goose/。
- 最终验证：`check_version.py` 通过（0.7.0 全一致）；.codebuddy/AGENTS.md 含 11 工具行；tools.json 版本 0.7.0、generate_quiz 描述含"文言文释义"。
- 注：未 git commit / 打 Tag；按 AGENTS 规则需推送 main 且 CI 通过后再打 Tag。

## 续：pre-commit 钩子旧目录引用导致提交被拦
- 现象：用户提交被 `pre-commit` 拒绝，报 `.codebuddy/AGENTS.md 与真相源 .agents/AGENTS.md 不一致`（cat: .agents/AGENTS.md: No such file or directory）。
- 根因：`.git/hooks/pre-commit` 是陈旧安装副本，仍指向已重命名的 `.agents/`；重装后发现源码 `scripts/pre-commit` 自身有 bug——重命名 `.agents/`→`vocabcraft.plugin/` 时误把真相源写成 `.vocabcraft.plugin/`（多了一个前导点，因原 `.agents` 是隐藏目录带点），导致 `gen_to_src` 与反向匹配路径全部失效（`cat` 找不到文件、所有副本误判不一致）。
- 修复：`scripts/pre-commit` 全局将 `.vocabcraft.plugin` → `vocabcraft.plugin`（去掉多余前导点），并重新 `Copy-Item scripts/pre-commit .git/hooks/pre-commit` 重装钩子。
- 验证：重装后 0 处 `.vocabcraft.plugin` 引用；以当前暂存区（含 0.7.0 改动 + 四平台镜像 + vocabcraft.plugin 真相源）运行钩子，**HOOK_EXIT 0（无违规）**，可正常提交。
- 教训：目录重命名（`.agents`→`vocabcraft.plugin`）时，所有带前导点的旧路径须同步去掉点；安装步骤 [6/5] 须随真相源改动重跑。
