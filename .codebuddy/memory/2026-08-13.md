# 2026-08-13

## generate-platform-configs.py 补齐 goose 分支
- 为 `scripts/generate-platform-configs.py` 新增 `generate_goose()`，使 `goose.json` 也由脚本统一生成（此前为手动维护）。
- `main()` 现写入 trae/opencode/codebuddy/goose 四个 runtime 配置。
- goose 用原生 `extensions` schema，`--directory` 相对路径 `vocabcraft-mcp`，由 `generate-goose-config.py` 解析为绝对路径写入 `.goose/config.yaml`。
- 验证：生成结果与原有手动文件一致，无 lint 错误。

## AAIF 声明文件改为脚本生成（消除死配置）
- 用户确认走 **AAIF 合规路线**：保留 `.agents/` 下 `tools.json`/`triggers.json`/`workflows.json` 三个 AAIF 标准声明文件，但改为由脚本从真实源生成（此前为手工维护、会漂移、无消费者）。
- 新增 `scripts/generate-aaif-declarations.py`：`tools.json` 自省实时 MCP server（`vocabcraft_mcp.server.list_tools()`）；`triggers.json` 聚合各 Skill 的 `When to Use` 触发词 + `/<skill>` 命令别名；`workflows.json` 聚合各 Skill 实际引用的 MCP 工具（按出现顺序）。
- 接入 `sync-agent-configs.{ps1,sh}`（同步前重生，需 uv）与 `package.json`（`generate-configs` 与新增 `generate-declarations`）。
- 修正原 `triggers.json` 命令格式 bug：旧 `/vocabcraft\s+(capture...)` → 正确 `/capture`。
- README/DEPLOY/CHANGELOG 注明其为脚本生成、供 `agents publish` 消费。ruff 通过，JSON 合法。
